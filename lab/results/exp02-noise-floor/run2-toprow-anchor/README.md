# run 2 — hold-anchored, top-row hold, per-frame ratio form (2026-07-06)

Frozen auto-outputs of the second exp02 run: variant D anchored to the purple
top-row hold (`anchors-toprow.json`, the exact clicks used), relative form
still the per-frame D/torso ratio. Interpretation: `../findings-hold-anchor.md`.

Superseded by the run at the directory root, which (a) redefines the relative
form as per-take median D ÷ per-take median torso (pre-registered before the
near-anchor experiment) and (b) moves the anchor to a near-working-zone hold.
